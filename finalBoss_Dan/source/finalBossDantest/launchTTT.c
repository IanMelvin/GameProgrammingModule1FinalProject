#include "finalBossDanTest/launchTTT.h"
#include "finalBoss_Dan/finalBossLoki.h"


#include <stdio.h>

#define BOARD_WIDTH 3
#define BOARD_HEIGHT 3

int launchTicTacToe()
{
	char values[BOARD_WIDTH * BOARD_HEIGHT];

	char* ptr;

	ptr = values;

	fillArray(ptr, BOARD_WIDTH * BOARD_HEIGHT);

	startGame(ptr);

	return 0;
}



int startGame(char *ptr)
{
	int gameLoop = 0, xCoord = 0, yCoord = 0, players = 1;

	while (gameLoop = 0)
	{
		xCoord = getCpprd
	}

	return 0;
}